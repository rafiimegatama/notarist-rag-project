package com.notarist.kase.domain.valueobject;

/** What a bundle is for. A case typically has several. */
public enum BundleType {
    IDENTITY,
    LAND_CERTIFICATE,
    SUPPORTING,
    DRAFT_OUTPUT
}
