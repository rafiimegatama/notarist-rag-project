package com.notarist.verification.domain.valueobject;

/** The verification checklist categories, per the domain's fixed set. */
public enum ChecklistCategory {
    AUTHORITY,
    DIRECTOR,
    IDENTITY,
    DOCUMENT_CONSISTENCY,
    CERTIFICATE,
    SKMHT_DEADLINE,
    NPWP_NIK,
    SIGNATURE_PRESENCE,
    SUPPORTING_DOCUMENTS
}
